# Pose Lab App Surface

This directory is the future home for browser UI entrypoints and app-shell code.

Current runtime files remain in the repo root and `src/` during migration:

- `pose-lab.html`
- `pose-critique.html`
- `src/pose-lab.js`
- `src/pose-lab.css`

Do not move those files until compatibility wrappers and case verification prove the route still renders.
